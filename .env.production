# Empty = same-origin; Vercel rewrites proxy to Railway (see vercel.json).
# Or set full URL with https:// (required — without it you get HTTP 405):
# VITE_API_BASE_URL=https://backend-production-5e544.up.railway.app
VITE_API_BASE_URL=
