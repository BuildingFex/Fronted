# Empty = same-origin via Vite proxy.
VITE_API_BASE_URL=
# API remota (Railway). Para Mercado Pago, configura las variables MP en Railway (ver appsettings.Local.json.example).
VITE_API_PROXY_TARGET=https://backend-production-5e544.up.railway.app
# Backend local (requiere MySQL + appsettings.Local.json con credenciales MP):
# VITE_API_PROXY_TARGET=http://localhost:5001
